# termicalc
simple calculator in terminal

## what can it do?
perfome operation on two numbers with add, sub, multi, divis

## how to compilate
download Makefile and compile with command "make", or enter command

```bash
gcc main.c -o main
```
